<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Employee | RSR HRMS</title>

  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet" />

  <style>
    :root {
      --rsr-blue: #0052cc;
      --rsr-text: #222222;
      --rsr-text-light: #717171;
      --rsr-border: #ebebeb;
      --rsr-bg: #f8fafc;
      --rsr-white: #ffffff;
    }

    body {
      background-color: var(--rsr-bg);
      font-family: 'Plus Jakarta Sans', sans-serif;
      color: var(--rsr-text);
      letter-spacing: -0.01em;
    }

    /* Navbar */
    .navbar {
      background: var(--rsr-white) !important;
      border-bottom: 1px solid var(--rsr-border);
      padding: 1rem 0;
    }

    .navbar-brand {
      font-weight: 800;
      color: var(--rsr-blue) !important;
    }

    /* Form Container */
    .employee-container {
      max-width: 800px;
      margin: 60px auto;
      background: var(--rsr-white);
      border-radius: 24px;
      padding: 48px;
      border: 1px solid var(--rsr-border);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.02);
    }

    .form-section-title {
      font-size: 1.5rem;
      font-weight: 800;
      color: var(--rsr-text);
      letter-spacing: -0.02em;
    }

    /* Form Elements */
    label.form-label {
      font-weight: 700;
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--rsr-text-light);
      margin-bottom: 8px;
    }

    .form-control,
    .form-select {
      border-radius: 12px;
      padding: 12px 16px;
      font-size: 0.95rem;
      border: 1px solid var(--rsr-border);
      transition: all 0.2s ease;
      background-color: #fbfcfd;
    }

    .form-control:focus,
    .form-select:focus {
      border-color: var(--rsr-blue);
      background-color: var(--rsr-white);
      box-shadow: 0 0 0 4px rgba(0, 82, 204, 0.05);
      outline: none;
    }

    /* Buttons */
    .btn-save {
      background-color: var(--rsr-blue);
      color: white;
      border: none;
      border-radius: 12px;
      padding: 16px;
      font-size: 1rem;
      font-weight: 700;
      transition: all 0.2s ease;
    }

    .btn-save:hover {
      background-color: #0043a8;
      transform: translateY(-1px);
      box-shadow: 0 8px 20px rgba(0, 82, 204, 0.15);
    }

    .header-badge {
      display: inline-block;
      padding: 6px 12px;
      background: #f0f7ff;
      color: var(--rsr-blue);
      border-radius: 8px;
      font-weight: 800;
      font-size: 0.7rem;
      text-transform: uppercase;
      margin-bottom: 12px;
    }
  </style>
</head>

<body>

  <nav class="navbar navbar-expand-lg sticky-top">
    <div class="container">
      <a class="navbar-brand" href="/hr/dashboard">
        <i class="fa-solid fa-users-gear me-2"></i> RSR HRMS
      </a>
      <div class="ms-auto">
        <a href="/hr/dashboard" class="btn btn-outline-dark btn-sm rounded-pill px-4 fw-bold">
          <i class="fa-solid fa-arrow-left me-2"></i> Back to Dashboard
        </a>
      </div>
    </div>
  </nav>

  <div class="employee-container">
    <div class="text-center mb-5">
      <div class="header-badge">Human Capital Management</div>
      <h2 class="form-section-title">Add New Employee Profile</h2>
      <p class="text-muted small">Enter the professional and personal details to create a new record in the corporate
        directory.</p>
    </div>

    <form method="POST" action="/employee/add" autocomplete="off">
      <div class="row g-4">

        <div class="col-md-6">
          <label class="form-label">Employee ID / Code</label>
          <input type="text" name="employeeCode" class="form-control" placeholder="e.g., RSR-2026-001" required>
        </div>
        <div class="col-md-6">
          <label class="form-label">Contact Number</label>
          <input type="text" name="phoneNumber" class="form-control" placeholder="+91 00000 00000" required>
        </div>

        <div class="col-md-6">
          <label class="form-label">First Name</label>
          <input type="text" name="firstName" class="form-control" required>
        </div>
        <div class="col-md-6">
          <label class="form-label">Last Name</label>
          <input type="text" name="lastName" class="form-control">
        </div>

        <div class="col-md-6">
          <label class="form-label">Date of Birth</label>
          <input type="date" name="dob" class="form-control" required>
        </div>
        <div class="col-md-6">
          <label class="form-label">Official Email Address</label>
          <input type="email" name="email" class="form-control" placeholder="employee@rsr-corp.com" required>
        </div>

        <div class="col-md-6">
          <label class="form-label">Assign Department</label>
          <select name="department" class="form-select" required>
            <option value="SALES">SALES</option>
            <option value="IT">IT</option>
            <option value="HR">HR</option>
            <option value="PROCUREMENT">PROCUREMENT</option>
            <option value="LOGISTICS">LOGISTICS</option>
            <option value="OPERATIONS">OPERATIONS</option>
            <option value="STORES">STORES</option>
            <option value="ACCOUNTS">ACCOUNTS</option>
          </select>
        </div>
        <div class="col-md-6">
          <label class="form-label">Designation / Role</label>
          <select name="designation" class="form-select" required>
            <option value="STAFF">STAFF</option>
            <option value="MANAGER">MANAGER</option>
          </select>
        </div>

        <div class="col-md-6">
          <label class="form-label">Employment Classification</label>
          <select name="employmentType" class="form-select">
            <option>Full-Time</option>
            <option>Part-Time</option>
            <option>Contract</option>
            <option>Intern</option>
          </select>
        </div>
        <div class="col-md-6">
          <label class="form-label">Monthly Salary (₹)</label>
          <input type="number" name="salary" class="form-control" placeholder="e.g. 50000" required min="0">
        </div>
        <div class="col-md-6">
          <label class="form-label">Onboarding Date</label>
          <input type="date" name="joiningDate" class="form-control" required>
        </div>

        <div class="col-12">
          <label class="form-label">Assigned Work Location</label>
          <div class="input-group">
            <span class="input-group-text bg-light border-end-0"><i
                class="fa-solid fa-location-dot text-muted"></i></span>
            <input type="text" name="workLocation" class="form-control border-start-0" value="Hyderabad">
          </div>
        </div>
      </div>

      <div class="d-grid mt-5">
        <button type="submit" class="btn btn-save shadow-sm">
          <i class="fa-solid fa-user-plus me-2"></i> Register Employee & Create Profile
        </button>
      </div>
    </form>
  </div>

  <footer class="text-center py-5">
    <p class="text-muted small">© 2026 RSR HRMS • Centralized Personnel Registry</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>