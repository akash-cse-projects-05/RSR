<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Employee Login | RSR HRMS</title>

  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

  <style>
    :root {
      --rsr-blue: #0052cc;
      --rsr-text: #222222;
      --rsr-text-light: #717171;
      --rsr-border: #ebebeb;
      --rsr-bg: #f7f9fc;
      /* Soft grey background to make the white card pop */
    }

    body {
      background-color: var(--rsr-bg);
      font-family: 'Plus Jakarta Sans', sans-serif;
      color: var(--text-main);
      letter-spacing: -0.01em;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    .login-wrapper {
      width: 100%;
      max-width: 450px;
      padding: 20px;
    }

    .login-card {
      background: #ffffff;
      border: 1px solid var(--rsr-border);
      border-radius: 24px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.06);
      padding: 40px;
    }

    .brand-logo {
      color: var(--rsr-blue);
      font-size: 2rem;
      margin-bottom: 10px;
    }

    .login-header h2 {
      font-weight: 800;
      font-size: 1.75rem;
      letter-spacing: -0.04em;
      margin-bottom: 8px;
    }

    .login-header p {
      color: var(--rsr-text-light);
      font-size: 0.95rem;
      margin-bottom: 32px;
    }

    /* Form Controls */
    .form-label {
      font-weight: 700;
      font-size: 0.85rem;
      margin-bottom: 8px;
      color: var(--rsr-text);
    }

    .input-group {
      border: 1px solid var(--rsr-border);
      border-radius: 12px;
      overflow: hidden;
      transition: border-color 0.2s;
    }

    .input-group:focus-within {
      border-color: var(--rsr-blue);
      box-shadow: 0 0 0 3px rgba(0, 82, 204, 0.1);
    }

    .input-group-text {
      background: transparent;
      border: none;
      color: var(--rsr-text-light);
      padding-left: 16px;
    }

    .form-control {
      border: none;
      padding: 14px 16px;
      font-size: 0.95rem;
    }

    .form-control:focus {
      box-shadow: none;
    }

    /* Button */
    .btn-login {
      background-color: var(--rsr-blue);
      color: white;
      border: none;
      border-radius: 12px;
      padding: 14px;
      font-weight: 700;
      font-size: 1rem;
      margin-top: 10px;
      transition: all 0.2s;
    }

    .btn-login:hover {
      background-color: #0043a8;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(0, 82, 204, 0.2);
    }

    .footer-text {
      text-align: center;
      margin-top: 32px;
      font-size: 0.85rem;
      color: var(--rsr-text-light);
    }

    .alert-custom {
      border-radius: 12px;
      font-size: 0.85rem;
      font-weight: 600;
      border: none;
      background-color: #fff1f0;
      color: #d85140;
    }
  </style>
</head>

<body>

  <div class="login-wrapper">
    <div class="login-card">

      <div class="login-header text-center">
        <div class="brand-logo">
          <i class="fa-solid fa-paper-plane"></i>
        </div>
        <h2>Employee Login</h2>
        <p>Enter your credentials to access the RSR Aviation portal</p>
      </div>

      <% if (typeof error !=="undefined" ) { %>
        <div class="alert alert-custom d-flex align-items-center mb-4">
          <i class="fa-solid fa-circle-exclamation me-2"></i>
          <%= error %>
        </div>
        <% } %>

          <form method="POST" action="/auth/login" novalidate class="needs-validation">

            <div class="mb-3">
              <label class="form-label">Employee Code</label>
              <div class="input-group">
                <span class="input-group-text">
                  <i class="fa-solid fa-id-badge"></i>
                </span>
                <input type="text" name="username" class="form-control" placeholder="RSR001" required>
              </div>
              <div class="invalid-feedback">Please enter your employee code.</div>
            </div>

            <div class="mb-4">
              <div class="d-flex justify-content-between">
                <label class="form-label">Password</label>
                <a href="/auth/forgot-password" class="text-decoration-none small fw-bold"
                  style="color: var(--rsr-blue);">Forgot?</a>
              </div>
              <div class="input-group">
                <span class="input-group-text">
                  <i class="fa-solid fa-lock"></i>
                </span>
                <input type="password" name="password" class="form-control" placeholder="••••••••" required>
              </div>
              <div class="invalid-feedback">Password is required to log in.</div>
            </div>

            <div class="d-grid">
              <button class="btn btn-login">
                Sign In
              </button>
            </div>

          </form>

          <div class="footer-text">
            <i class="fa-solid fa-shield-halved me-1"></i> Authorized Personnel Only
            <div class="mt-3">
              <a href="/hr/hr-login" class="text-secondary small text-decoration-none fw-bold">
                <i class="fa-solid fa-user-shield me-1"></i> HR / Admin Login
              </a>
            </div>
          </div>
    </div>

    <div class="text-center mt-4">
      <p class="small text-muted">© 2026 RSR Aviation Group • Precision Management</p>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    (() => {
      'use strict'
      const forms = document.querySelectorAll('.needs-validation')
      Array.from(forms).forEach(form => {
        form.addEventListener('submit', event => {
          if (!form.checkValidity()) {
            event.preventDefault()
            event.stopPropagation()
          }
          form.classList.add('was-validated')
        }, false)
      })
    })()
  </script>

</body>

</html>