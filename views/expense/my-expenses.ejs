<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Expenses | RSR HRMS</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

    <style>
        :root {
            --rsr-blue: #0052cc;
            --rsr-text: #222222;
            --rsr-bg: #f8f9fa;
        }

        body {
            background-color: var(--rsr-bg);
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: var(--rsr-text);
        }

        .navbar {
            background: #fff !important;
            border-bottom: 1px solid #ebebeb;
            padding: 1rem 0;
        }

        .navbar-brand {
            font-weight: 800;
            color: var(--rsr-blue) !important;
        }

        .page-header {
            padding: 40px 0;
            background: #fff;
            border-bottom: 1px solid #ebebeb;
            margin-bottom: 30px;
        }

        .expense-card {
            border: 1px solid #ebebeb;
            border-radius: 16px;
            background: #fff;
            padding: 20px;
            margin-bottom: 15px;
        }

        .badge-status {
            padding: 6px 12px;
            border-radius: 20px;
            font-weight: 700;
            font-size: 0.75rem;
            text-transform: uppercase;
        }

        .bg-pending {
            background: #fff9db;
            color: #f08c00;
        }

        .bg-approved {
            background: #e6fcf5;
            color: #0ca678;
        }

        .bg-rejected {
            background: #fff5f5;
            color: #ff6b6b;
        }

        .btn-main {
            background-color: var(--rsr-blue);
            color: white;
            border-radius: 10px;
            font-weight: 700;
            padding: 10px 24px;
            text-decoration: none;
        }

        .btn-main:hover {
            background-color: #0043a8;
            color: white;
        }
    </style>
</head>

<body>
    <%- include('../partials/anti-cache') %>

        <nav class="navbar navbar-expand-lg sticky-top">
            <div class="container">
                <a class="navbar-brand" href="/dashboard"><i class="fa-solid fa-paper-plane me-2"></i> RSR AVIATION</a>
                <div class="ms-auto"><a href="/dashboard"
                        class="btn btn-outline-dark btn-sm rounded-pill px-3">Dashboard</a></div>
            </div>
        </nav>

        <header class="page-header">
            <div class="container d-flex justify-content-between align-items-center">
                <div>
                    <p class="text-primary fw-bold small mb-1" style="letter-spacing: 0.1em;">REIMBURSEMENTS</p>
                    <h1 class="fw-bold mb-0">My Expenses</h1>
                </div>
                <a href="/expense/apply" class="btn-main"><i class="fa-solid fa-plus me-2"></i>New Claim</a>
            </div>
        </header>

        <div class="container pb-5">
            <% if (expenses && expenses.length> 0) { %>
                <div class="row">
                    <% expenses.forEach(exp=> { %>
                        <div class="col-md-6 col-lg-4">
                            <div class="expense-card h-100 d-flex flex-column">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <span class="badge bg-light text-dark border">
                                        <%= exp.type %>
                                    </span>
                                    <span
                                        class="badge-status <%= exp.status === 'Approved' ? 'bg-approved' : exp.status === 'Rejected' ? 'bg-rejected' : 'bg-pending' %>">
                                        <%= exp.status %>
                                    </span>
                                </div>

                                <h3 class="fw-bold mb-1">₹<%= exp.amount.toLocaleString() %>
                                </h3>
                                <p class="text-muted small mb-3">
                                    <%= new Date(exp.date).toLocaleDateString() %>
                                </p>
                                <p class="mb-3 text-secondary">
                                    <%= exp.description %>
                                </p>

                                <% if (exp.location && exp.location.lat) { %>
                                    <div class="mb-3 small text-muted"><i class="fa-solid fa-location-dot me-1"></i>
                                        Geo-tagged</div>
                                    <% } %>

                                        <div
                                            class="mt-auto d-flex justify-content-between align-items-center pt-3 border-top">
                                            <% if (exp.receiptPath) { %>
                                                <a href="<%= exp.receiptPath %>" target="_blank"
                                                    class="text-primary small fw-bold text-decoration-none"><i
                                                        class="fa-solid fa-paperclip me-1"></i> View Receipt</a>
                                                <% } else { %>
                                                    <span class="text-muted small">No Receipt</span>
                                                    <% } %>

                                                        <% if (exp.status==='Rejected' ) { %>
                                                            <span class="text-danger small fw-bold"
                                                                title="<%= exp.rejectionReason %>"><i
                                                                    class="fa-solid fa-circle-exclamation me-1"></i>
                                                                Rejected</span>
                                                            <% } %>
                                        </div>
                            </div>
                        </div>
                        <% }) %>
                </div>
                <% } else { %>
                    <div class="text-center py-5">
                        <i class="fa-solid fa-wallet text-muted fs-1 mb-3 opacity-50"></i>
                        <p class="text-muted">No expense claims found.</p>
                    </div>
                    <% } %>
        </div>

        <footer class="text-center py-5 mt-auto border-top">
            <p class="text-muted small">© 2026 RSR HRMS</p>
        </footer>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>