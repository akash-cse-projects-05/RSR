<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Allot Mission | RSR Aviation</title>

  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

  <style>
    :root {
      --rsr-blue: #0052cc;
      --rsr-text: #222222;
      --rsr-text-light: #717171;
      --rsr-border: #ebebeb;
      --rsr-bg: #f7f9fc;
    }

    body {
      background-color: var(--rsr-bg);
      font-family: 'Plus Jakarta Sans', sans-serif;
      color: var(--rsr-text);
      letter-spacing: -0.01em;
      min-height: 100vh;
    }

    .navbar {
      background: #fff !important;
      border-bottom: 1px solid var(--rsr-border);
      padding: 1rem 0;
    }

    .navbar-brand {
      font-weight: 800;
      color: var(--rsr-blue) !important;
    }

    .form-wrapper {
      padding: 60px 0;
      display: flex;
      justify-content: center;
    }

    .assignment-card {
      max-width: 800px;
      width: 100%;
      background: #fff;
      border: 1px solid var(--rsr-border);
      border-radius: 24px;
      padding: 40px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.04);
    }

    .form-label {
      font-weight: 700;
      font-size: 0.75rem;
      margin-bottom: 8px;
      color: var(--rsr-text-light);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .form-control,
    .form-select {
      border-radius: 12px;
      padding: 12px 16px;
      border: 1px solid var(--rsr-border);
      font-size: 0.95rem;
      transition: all 0.2s ease;
      background-color: #fff;
    }

    .form-control:focus,
    .form-select:focus {
      border-color: var(--rsr-blue);
      box-shadow: 0 0 0 4px rgba(0, 82, 204, 0.05);
    }

    .btn-main {
      background-color: var(--rsr-blue);
      color: white;
      border: none;
      border-radius: 12px;
      padding: 16px;
      font-weight: 700;
      width: 100%;
      margin-top: 20px;
      transition: transform 0.2s;
    }

    .btn-main:hover {
      background-color: #0043a8;
      transform: translateY(-1px);
    }

    .btn-back {
      color: var(--rsr-text-light);
      text-decoration: none;
      font-weight: 600;
      font-size: 0.9rem;
      display: inline-flex;
      align-items: center;
      margin-bottom: 25px;
    }

    .btn-back:hover {
      color: var(--rsr-blue);
    }

    .header-badge {
      display: inline-block;
      padding: 6px 12px;
      background: #f0f7ff;
      color: var(--rsr-blue);
      border-radius: 8px;
      font-weight: 800;
      font-size: 0.7rem;
      text-transform: uppercase;
      margin-bottom: 15px;
    }
  </style>
</head>

<body>
  <%- include('../partials/anti-cache') %>

    <nav class="navbar navbar-expand-lg sticky-top">
      <div class="container">
        <a class="navbar-brand" href="/dashboard">
          <i class="fa-solid fa-paper-plane me-2"></i> RSR AVIATION
        </a>
      </div>
    </nav>

    <div class="container form-wrapper">
      <div class="assignment-card">
        <a href="/department/<%= department %>" class="btn-back">
          <i class="fa-solid fa-arrow-left me-2"></i> Return to Operations
        </a>

        <div class="header-badge">Mission Control</div>
        <h2 class="fw-bold mb-1">Allot Task / Training</h2>
        <p class="text-muted mb-4">Assign operational duties or certification modules to department staff.</p>



        <form method="POST" action="/department/<%= department %>/task">

          <div class="row g-3 mb-3">
            <div class="col-md-8">
              <label class="form-label">Mission Title</label>
              <input type="text" name="title" placeholder="e.g., Annual Safety Audit" class="form-control" required />
            </div>
            <div class="col-md-4">
              <label class="form-label">Assignment Type</label>
              <select name="type" class="form-select" required>
                <option value="Work">Operational (Work)</option>
                <option value="Training">Educational (Training)</option>
              </select>
            </div>
          </div>

          <div class="mb-3">
            <label class="form-label">Technical Description</label>
            <textarea name="description" placeholder="Provide clear objectives and requirements..." class="form-control"
              rows="4"></textarea>
          </div>

          <div class="mb-3">
            <label class="form-label">Assign To Personnel</label>
            <select name="assignedTo" class="form-select" required>
              <option value="" disabled selected>Select Staff Member...</option>
              <% employees.filter(e=> e.designation === 'STAFF').forEach(staff => { %>
                <option value="<%= staff._id %>">
                  <%= staff.firstName %>
                    <%= staff.lastName %>
                </option>
                <% }) %>
            </select>
          </div>

          <div class="row g-3 mb-3">
            <div class="col-md-6">
              <label class="form-label">Start Date</label>
              <input type="date" name="startDate" class="form-control" required />
            </div>
            <div class="col-md-6">
              <label class="form-label">Deadline / Due Date</label>
              <input type="date" name="deadline" class="form-control" required />
            </div>
          </div>

          <div class="mb-4">
            <label class="form-label">Reference Resources (URL)</label>
            <div class="input-group">
              <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-link text-muted"></i></span>
              <input type="url" name="resources" class="form-control border-start-0"
                placeholder="https://docs.rsr-aviation.com/resource" />
            </div>
          </div>

          <button type="submit" class="btn btn-main shadow-sm">
            <i class="fa-solid fa-plus me-2"></i> Deploy Mission / Training
          </button>
        </form>
      </div>
    </div>

    <footer class="text-center py-4">
      <p class="text-muted small">© 2026 RSR Aviation Group • Human Capital Allocation Portal</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>