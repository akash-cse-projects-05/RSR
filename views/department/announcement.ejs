<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Post Announcement | RSR Aviation</title>

  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

  <style>
    :root {
      --rsr-blue: #0052cc;
      --rsr-text: #222222;
      --rsr-text-light: #717171;
      --rsr-border: #ebebeb;
      --rsr-bg: #f7f9fc;
    }

    body {
      background-color: var(--rsr-bg);
      font-family: 'Plus Jakarta Sans', sans-serif;
      color: var(--rsr-text);
      letter-spacing: -0.01em;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Navbar */
    .navbar {
      background: #fff !important;
      border-bottom: 1px solid var(--rsr-border);
      padding: 1rem 0;
    }

    .navbar-brand {
      font-weight: 800;
      color: var(--rsr-blue) !important;
    }

    /* Centered Container */
    .form-wrapper {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px 20px;
    }

    .announcement-card {
      max-width: 600px;
      width: 100%;
      background: #fff;
      border: 1px solid var(--rsr-border);
      border-radius: 24px;
      padding: 40px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.04);
    }

    /* Form Styling */
    .form-label {
      font-weight: 700;
      font-size: 0.85rem;
      margin-bottom: 8px;
      color: var(--rsr-text);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .form-control {
      border-radius: 12px;
      padding: 14px 16px;
      border: 1px solid var(--rsr-border);
      font-size: 0.95rem;
      transition: all 0.2s ease;
    }

    .form-control:focus {
      border-color: var(--rsr-blue);
      box-shadow: 0 0 0 4px rgba(0, 82, 204, 0.05);
    }

    /* Buttons */
    .btn-main {
      background-color: var(--rsr-blue);
      color: white;
      border: none;
      border-radius: 12px;
      padding: 14px 28px;
      font-weight: 700;
      width: 100%;
      transition: transform 0.2s;
    }

    .btn-main:hover {
      background-color: #0043a8;
      transform: translateY(-1px);
    }

    .btn-back {
      background: transparent;
      color: var(--rsr-text-light);
      border: none;
      font-weight: 600;
      font-size: 0.9rem;
      display: inline-flex;
      align-items: center;
      text-decoration: none;
      margin-bottom: 20px;
      transition: color 0.2s;
    }

    .btn-back:hover {
      color: var(--rsr-blue);
    }

    .header-icon {
      width: 50px;
      height: 50px;
      background: #f0f7ff;
      color: var(--rsr-blue);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin-bottom: 20px;
    }
  </style>
</head>

<body>
  <%- include('../partials/anti-cache') %>

    <nav class="navbar navbar-expand-lg sticky-top">
      <div class="container">
        <a class="navbar-brand" href="/dashboard">
          <i class="fa-solid fa-paper-plane me-2"></i> RSR AVIATION
        </a>
      </div>
    </nav>

    <div class="form-wrapper">
      <div class="announcement-card">
        <a href="/department/<%= department %>" class="btn-back">
          <i class="fa-solid fa-arrow-left me-2"></i> Back to Dashboard
        </a>

        <div class="header-icon">
          <i class="fa-solid fa-bullhorn"></i>
        </div>

        <h2 class="fw-bold mb-1">Broadcasting</h2>
        <p class="text-muted mb-4">Post a new announcement to the <strong>
            <%= department %>
          </strong> department.</p>

        <form method="POST" action="/department/<%= department %>/announcement">
          <div class="mb-3">
            <label class="form-label">Subject Line</label>
            <input type="text" name="title" placeholder="e.g., Schedule Update: Flight Operations" class="form-control"
              required />
          </div>

          <div class="mb-4">
            <label class="form-label">Detailed Message</label>
            <textarea name="message" placeholder="Type your announcement here..." class="form-control" rows="5"
              required></textarea>
          </div>

          <button type="submit" class="btn btn-main shadow-sm">
            <i class="fa-solid fa-paper-plane me-2"></i> Publish Announcement
          </button>
        </form>
      </div>
    </div>

    <footer class="text-center py-4">
      <p class="text-muted small">© 2026 RSR Aviation • Secure Communications Internal</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>