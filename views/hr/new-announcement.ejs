<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Announcement | RSR HRMS</title>

  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

  <style>
    :root {
      --rsr-blue: #0052cc;
      --rsr-text: #222222;
      --rsr-text-light: #717171;
      --rsr-border: #ebebeb;
      --rsr-bg: #f7f9fc;
    }

    body {
      background-color: var(--rsr-bg);
      font-family: 'Plus Jakarta Sans', sans-serif;
      color: var(--rsr-text);
      letter-spacing: -0.01em;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Navbar */
    .navbar {
      background: #fff !important;
      border-bottom: 1px solid var(--rsr-border);
      padding: 1rem 0;
    }

    .navbar-brand {
      font-weight: 800;
      color: var(--rsr-blue) !important;
    }

    /* Centered Form Wrapper */
    .form-wrapper {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px 20px;
    }

    .announcement-card {
      max-width: 480px;
      width: 100%;
      background: #fff;
      border: 1px solid var(--rsr-border);
      border-radius: 24px;
      padding: 40px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.04);
    }

    /* Form Label Styling */
    .form-label {
      font-weight: 700;
      font-size: 0.75rem;
      margin-bottom: 8px;
      color: var(--rsr-text-light);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    /* Input & Select Styling */
    .form-control,
    .form-select {
      border-radius: 12px;
      padding: 12px 16px;
      border: 1px solid var(--rsr-border);
      font-size: 0.95rem;
      transition: all 0.2s ease;
      background-color: #fff;
    }

    .form-control:focus,
    .form-select:focus {
      border-color: var(--rsr-blue);
      box-shadow: 0 0 0 4px rgba(0, 82, 204, 0.05);
      outline: none;
    }

    /* Button Styling */
    .btn-main {
      background-color: var(--rsr-blue);
      color: white;
      border: none;
      border-radius: 12px;
      padding: 14px;
      font-weight: 700;
      width: 100%;
      transition: all 0.2s;
      margin-top: 10px;
    }

    .btn-main:hover {
      background-color: #0043a8;
      transform: translateY(-1px);
      box-shadow: 0 8px 20px rgba(0, 82, 204, 0.15);
    }

    .btn-back {
      color: var(--rsr-text-light);
      text-decoration: none;
      font-weight: 600;
      font-size: 0.9rem;
      display: inline-flex;
      align-items: center;
      margin-bottom: 25px;
      transition: color 0.2s;
    }

    .btn-back:hover {
      color: var(--rsr-blue);
    }

    .header-icon {
      width: 48px;
      height: 48px;
      background: #f0f7ff;
      color: var(--rsr-blue);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
      margin-bottom: 20px;
    }
  </style>
</head>

<body>
  <%- include('../partials/anti-cache') %>

    <nav class="navbar navbar-expand-lg sticky-top">
      <div class="container">
        <a class="navbar-brand" href="/hr/dashboard">
          <i class="fa-solid fa-user-check me-2"></i> RSR HRMS
        </a>
      </div>
    </nav>

    <div class="form-wrapper">
      <div class="announcement-card">
        <a href="/hr/dashboard" class="btn-back">
          <i class="fa-solid fa-arrow-left me-2"></i> Back to Dashboard
        </a>

        <div class="header-icon">
          <i class="fa-solid fa-bullhorn"></i>
        </div>

        <h3 class="fw-bold mb-1">Create Broadcast</h3>
        <p class="text-muted mb-4 small">Post a new update or critical notification to the staff notice board.</p>

        <form method="POST" action="/notice-board/new">
          <div class="mb-3">
            <label for="type" class="form-label">Message Priority / Type</label>
            <select name="type" id="type" required class="form-select">
              <option value="announcement">Standard Announcement</option>
              <option value="notification">Critical Notification</option>
            </select>
          </div>

          <div class="mb-3">
            <label for="title" class="form-label">Subject Title</label>
            <input type="text" name="title" id="title" placeholder="Enter a concise title" required
              class="form-control">
          </div>

          <div class="mb-4">
            <label for="message" class="form-label">Content</label>
            <textarea name="message" id="message" placeholder="Type your message here..." required class="form-control"
              rows="5"></textarea>
          </div>

          <button type="submit" class="btn-main shadow-sm">
            <i class="fa-solid fa-paper-plane me-2"></i> Post to Notice Board
          </button>
        </form>
      </div>
    </div>

    <footer class="text-center py-4 mt-auto">
      <p class="text-muted small">© 2026 RSR HRMS • Internal Communications Portal</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>